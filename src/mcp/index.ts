/**
 * MCP Server Module
 * 
 * Exports the Protokoll MCP server for use as a library.
 * For standalone execution, use the server.ts entry point directly.
 */

// Re-export server entry point (the main() function is self-contained)
export * from './server';
